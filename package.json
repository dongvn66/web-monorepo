{
  "name": "@dp/dp",
  "private": true,
  "devDependencies": {
    "lerna": "4.0.0",
    "typescript": "^4.3.5"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "clean": "lerna run clean --parallel"
  }
}
